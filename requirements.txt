# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.2

# Settings / config
pydantic==2.11.4
pydantic-settings==2.9.1

# HTTP client (used by FastAPI internally)
httpx==0.28.1

# Auth (JWT)
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4

# Database (async SQLAlchemy — swap for your preferred ORM)
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0          # PostgreSQL async driver

# LangChain core (always required)
langchain-core==0.3.60

# LLM providers — uncomment what you use
# langchain==0.3.25          # needed for init_chat_model (auto provider)
# langchain-aws==0.2.14      # Bedrock  → also needs: boto3
# langchain-openai==0.3.16   # OpenAI
# langchain-anthropic==0.3.15 # Anthropic

# Utilities
python-dotenv==1.1.0
